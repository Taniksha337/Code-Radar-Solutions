#include <stdio.h>

int main() {
    char ch;
    
    // Taking input for a single character (skip any newline character)
    scanf("%c", &ch);
    
    // Printing the character
    printf("%c\n", ch);
    
    return 0;
}
